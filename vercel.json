{
  "buildCommand": "npx vite build",
  "outputDirectory": "dist/public",
  "installCommand": "rm -f package-lock.json && npm install --registry https://registry.npmjs.org",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
